// import { pick } from 'lodash';

// export const getFullRooms = (rooms) => rooms;

// export const getRoomsForPublic = (rooms) => Object.entries(rooms).reduce((acc, [id, roomObject]) => {
//   acc[id] = {};
// }, {});
